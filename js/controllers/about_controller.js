'use strict';

define(['js/app'], function (app) {

    var injectParams = ['$scope'];

    var about_controller = function ($scope) {
    
        
    };

    about_controller.$inject = injectParams;

    app.register.controller('about_controller', about_controller);

});
